# 📖 دليل تثبيت Badhoven Stremio Addon

## ما هو Stremio؟

**Stremio** هو تطبيق لتشغيل الأفلام والمسلسلات يعمل على:
- 💻 Windows, macOS, Linux
- 📱 Android, iOS
- 🌐 Web Browser

## ما هو Addon؟

**Addon** (إضافة) هي وحدة توسعية تضيف مصادر جديدة للمحتوى في Stremio.

**Badhoven Addon** يوفر روابط من 5 مواقع مختلفة!

---

## 🚀 طريقة التثبيت السريعة

### الخطوة 1: تحميل Stremio

إذا لم يكن لديك Stremio:
1. اذهب إلى [stremio.com](https://www.stremio.com/)
2. حمّل النسخة المناسبة لجهازك
3. ثبّت البرنامج
4. أنشئ حساب أو سجّل دخول

### الخطوة 2: تشغيل الـ Addon

#### أ) على جهازك المحلي (للاستخدام الشخصي)

1. **تأكد من تثبيت Node.js**
   - اذهب إلى [nodejs.org](https://nodejs.org/)
   - حمّل وثبّت النسخة LTS

2. **فك ضغط ملف الـ Addon**
   - فك ضغط `badhoven-stremio-addon.zip`
   - ستحصل على مجلد `badhoven-stremio-addon`

3. **افتح Terminal/Command Prompt**
   - **Windows**: اضغط `Win + R`، اكتب `cmd`، اضغط Enter
   - **Mac**: اضغط `Cmd + Space`، اكتب `terminal`
   - **Linux**: اضغط `Ctrl + Alt + T`

4. **انتقل لمجلد الـ Addon**
   ```bash
   cd path/to/badhoven-stremio-addon
   ```
   مثال:
   ```bash
   cd C:\Users\YourName\Downloads\badhoven-stremio-addon
   ```

5. **ثبّت المكتبات المطلوبة**
   ```bash
   npm install
   ```
   انتظر حتى تنتهي العملية (قد تستغرق دقيقة)

6. **شغّل الـ Addon**
   ```bash
   npm start
   ```

7. **ستظهر رسالة مثل هذه:**
   ```
   ╔═══════════════════════════════════════════════════════════╗
   ║   🎬 Badhoven Stremio Addon                              ║
   ║   Server running on: http://127.0.0.1:7000               ║
   ║   Install URL: http://127.0.0.1:7000/manifest.json       ║
   ╚═══════════════════════════════════════════════════════════╝
   ```

**⚠️ مهم**: اترك هذه النافذة مفتوحة! إذا أغلقتها سيتوقف الـ Addon

#### ب) على الإنترنت (للمشاركة مع الآخرين)

يمكنك نشر الـ Addon على الإنترنت مجاناً باستخدام:

**1. Heroku (مجاني)**
- اذهب إلى [heroku.com](https://heroku.com)
- أنشئ حساب
- اتبع [هذا الدليل](https://devcenter.heroku.com/articles/deploying-nodejs)

**2. Railway (مجاني)**
- اذهب إلى [railway.app](https://railway.app)
- سجّل دخول بحساب GitHub
- اضغط "New Project"
- اختر "Deploy from GitHub repo"

**3. Vercel (مجاني)**
- اذهب إلى [vercel.com](https://vercel.com)
- سجّل دخول
- اسحب مجلد الـ Addon للموقع

بعد النشر، ستحصل على رابط مثل:
```
https://badhoven-addon.herokuapp.com/manifest.json
```

### الخطوة 3: تثبيت الـ Addon في Stremio

1. **افتح تطبيق Stremio**

2. **اذهب إلى Addons**
   - اضغط على أيقونة الـ Addons في الأعلى (🧩)
   - أو من القائمة اختر "Addons"

3. **افتح Community Addons**
   - في صفحة الـ Addons
   - ابحث عن قسم "Community Addons"
   - أو اضغط على أيقونة الـ URL في الأعلى

4. **أدخل رابط الـ Addon**
   
   **إذا كنت تشغله محلياً:**
   ```
   http://127.0.0.1:7000/manifest.json
   ```
   
   **إذا نشرته على الإنترنت:**
   ```
   https://your-addon-url.com/manifest.json
   ```

5. **اضغط Install**

6. **تم! 🎉**

---

## 🎬 كيفية الاستخدام

### مشاهدة فيلم

1. افتح Stremio
2. ابحث عن فيلم (مثلاً: "Oppenheimer")
3. اضغط على الفيلم
4. ستظهر لك قائمة بالمصادر
5. ابحث عن مصادر من **Badhoven**:
   - 🎬 VidSrc
   - 🎬 VidSrc.to
   - 🎬 2Embed
   - 🎬 SuperEmbed
   - 🎬 MoviesAPI
6. اضغط على المصدر الذي تريده
7. استمتع! 🍿

### مشاهدة مسلسل

1. ابحث عن مسلسل (مثلاً: "Breaking Bad")
2. اضغط على المسلسل
3. اختر الموسم
4. اختر الحلقة
5. اختر مصدر من Badhoven
6. استمتع! 📺

---

## ❓ حل المشاكل الشائعة

### المشكلة: الـ Addon لا يظهر في Stremio

**الحل:**
1. تأكد أن الـ Addon يعمل (النافذة مفتوحة)
2. تأكد من الرابط: `http://127.0.0.1:7000/manifest.json`
3. جرب إعادة تشغيل Stremio
4. جرب إزالة الـ Addon وإعادة تثبيته

### المشكلة: "npm: command not found"

**الحل:**
- لم تثبت Node.js بعد
- حمّله من [nodejs.org](https://nodejs.org/)
- أعد تشغيل Terminal بعد التثبيت

### المشكلة: لا توجد روابط متاحة

**الحل:**
- بعض المحتوى قد لا يكون متاحاً
- جرب فيلم أو مسلسل آخر
- تأكد من اتصالك بالإنترنت

### المشكلة: الروابط لا تعمل

**الحل:**
- جرب مصدر آخر من الـ Addon
- بعض المصادر قد تكون معطلة مؤقتاً
- تأكد من سرعة الإنترنت

### المشكلة: "Port 7000 is already in use"

**الحل:**
- البورت مستخدم من برنامج آخر
- غيّر البورت:
  ```bash
  PORT=8080 npm start
  ```
- ثم استخدم: `http://127.0.0.1:8080/manifest.json`

---

## 🔧 إعدادات متقدمة

### تغيير البورت

افتح ملف `addon.js` وعدّل السطر:
```javascript
const PORT = process.env.PORT || 7000; // غيّر 7000 إلى البورت الذي تريده
```

أو شغّل بهذه الطريقة:
```bash
PORT=8080 npm start
```

### تعطيل بعض المصادر

افتح `providers/index.js` وعلّق على المصادر:
```javascript
module.exports = [
    vidsrc,
    // vidsrcTo,  // معطّل
    twoEmbed,
    superEmbed,
    // moviesApi  // معطّل
];
```

### تشغيل تلقائي عند بدء الجهاز

**Windows:**
1. اضغط `Win + R`
2. اكتب `shell:startup`
3. أنشئ ملف `start-addon.bat`:
   ```batch
   @echo off
   cd C:\path\to\badhoven-stremio-addon
   npm start
   ```

**Mac/Linux:**
استخدم `cron` أو `systemd`

---

## 📱 الاستخدام على الهاتف

### Android

1. ثبّت Stremio من Google Play
2. تأكد أن الـ Addon يعمل على جهاز الكمبيوتر
3. في Stremio على الهاتف، أدخل رابط الـ Addon
4. **ملاحظة**: استخدم IP جهازك بدلاً من `127.0.0.1`
   
   مثال:
   ```
   http://192.168.1.100:7000/manifest.json
   ```
   
   لمعرفة IP جهازك:
   - **Windows**: `ipconfig` في CMD
   - **Mac/Linux**: `ifconfig` في Terminal

### iOS

نفس الخطوات، لكن قد تحتاج لنشر الـ Addon على الإنترنت

---

## 🌐 نشر على الإنترنت (تفصيلي)

### Heroku

1. **ثبّت Heroku CLI**
   ```bash
   npm install -g heroku
   ```

2. **سجّل دخول**
   ```bash
   heroku login
   ```

3. **أنشئ تطبيق**
   ```bash
   cd badhoven-stremio-addon
   git init
   heroku create badhoven-addon
   ```

4. **انشر**
   ```bash
   git add .
   git commit -m "Initial commit"
   git push heroku master
   ```

5. **احصل على الرابط**
   ```bash
   heroku open
   ```
   أضف `/manifest.json` للرابط

### Railway

1. اذهب إلى [railway.app](https://railway.app)
2. اضغط "Start a New Project"
3. اختر "Deploy from GitHub repo"
4. اختر مجلد الـ Addon
5. انتظر حتى ينتهي النشر
6. احصل على الرابط من Settings

### Vercel

1. ثبّت Vercel CLI
   ```bash
   npm i -g vercel
   ```

2. انشر
   ```bash
   cd badhoven-stremio-addon
   vercel
   ```

3. اتبع التعليمات

---

## 💡 نصائح

1. **للاستخدام الشخصي**: التشغيل المحلي أفضل وأسرع
2. **للمشاركة**: انشر على الإنترنت
3. **للأداء الأفضل**: استخدم اتصال إنترنت سريع
4. **للخصوصية**: التشغيل المحلي أكثر خصوصية

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. راجع قسم "حل المشاكل" أعلاه
2. تأكد من تثبيت Node.js بشكل صحيح
3. تأكد من اتصالك بالإنترنت
4. جرب إعادة تشغيل كل شيء

---

## ⚠️ ملاحظات مهمة

- الـ Addon لا يستضيف أي محتوى
- جميع الروابط من مصادر خارجية
- أنت مسؤول عن الالتزام بالقوانين المحلية
- الـ Addon مجاني ومفتوح المصدر

---

**صُنع بـ ❤️ بواسطة badhoven**

**استمتع بالمشاهدة! 🎬🍿**
